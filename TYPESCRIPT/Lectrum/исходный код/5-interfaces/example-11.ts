{
    type NumberDictionary = {
        [index: number]: number;
        // length: number;
    }

    // let obj: NumberDictionary = {
    //     length: 1,
    //     1: 1
    // };
    const t: Array<string> = ['sd'];
    t.map()
    let obj: NumberDictionary = [1,2];
    obj.map()
}
