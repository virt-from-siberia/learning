import { axios } from "../../core";

export default {
    //BUG://
    // getAllByDialogId: (id) => axios.get("/messages?_id=" + id),
    // getAllByDialogId: (id) => {
    //     //FIXME: CONSOLE LOG ===========>
    //     console.log(id);
    //     return axios.get("/messages?_id" + id);
    // },
};
