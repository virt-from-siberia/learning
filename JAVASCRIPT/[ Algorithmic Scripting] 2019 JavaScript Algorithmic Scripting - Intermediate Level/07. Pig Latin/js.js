// BASIC

function translate() {
    let pigLating = '';
    let regex = /[aeiou]/gi;

    if (str[0].match(regex)) {
        pigLating = str + 'way';
    } else {
        let vowelIndice = indexOf(str.match(regex));
    }
}

console.log(translate('consonant'));
//INTERMEDIADE



//ADVANCED


//ADVANCED 2